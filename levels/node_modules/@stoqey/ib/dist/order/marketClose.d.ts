import { ACTION } from '../interfaces.share';
export declare function marketClose(action: ACTION, quantity: number, transmitOrder?: boolean): {
    action: ACTION;
    orderType: string;
    totalQuantity: number;
    transmit: boolean;
};
