/**
 * Index contract
 * @param symbol
 * @param currency
 * @param exchange
 */
export declare function index(symbol: string, currency?: string, exchange?: string): {
    currency: string;
    exchange: string;
    secType: string;
    symbol: string;
};
