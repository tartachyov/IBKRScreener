import { ACTION } from '../interfaces.share';
export declare function stop(action: ACTION, quantity: number, price: number, transmitOrder?: boolean, parentId?: number, tif?: string): {
    action: ACTION;
    auxPrice: number;
    orderType: string;
    totalQuantity: number;
    transmit: boolean;
    parentId: number;
    tif: string;
};
