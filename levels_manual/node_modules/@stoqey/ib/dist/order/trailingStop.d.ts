import { ACTION } from '../interfaces.share';
export declare function trailingStop(action: ACTION, quantity: number, auxPrice: number, tif?: string, transmitOrder?: boolean, parentId?: number): {
    action: ACTION;
    totalQuantity: number;
    orderType: string;
    auxPrice: number;
    tif: string;
    transmit: boolean;
    parentId: number;
};
