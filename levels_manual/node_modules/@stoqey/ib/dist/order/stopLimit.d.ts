import { ACTION } from '../interfaces.share';
export declare function stopLimit(action: ACTION, quantity: number, limitPrice: number, stopPrice: number, transmitOrder?: boolean, parentId?: number, tif?: string): {
    action: ACTION;
    lmtPrice: number;
    auxPrice: number;
    orderType: string;
    totalQuantity: number;
    transmit: boolean;
    parentId: number;
    tif: string;
};
