import { ACTION } from '../interfaces.share';
export declare function market(action: ACTION, quantity: number, transmitOrder?: boolean, goodAfterTime?: string, goodTillDate?: string): {
    action: ACTION;
    orderType: string;
    totalQuantity: number;
    transmit: boolean;
    goodAfterTime: string;
    goodTillDate: string;
};
