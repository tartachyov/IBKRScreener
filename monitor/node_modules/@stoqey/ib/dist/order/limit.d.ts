import { ACTION } from '../interfaces.share';
export declare function limit(action: ACTION, quantity: number, price: number, transmitOrder?: boolean): {
    action: ACTION;
    lmtPrice: number;
    orderType: string;
    totalQuantity: number;
    transmit: boolean;
};
