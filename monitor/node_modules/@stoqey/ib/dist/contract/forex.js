"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// Between two currencies,
// Whatever currency comes first should be in "symbol" and the other one must be in "currency".
// EUR GBP AUD USD TRY ZAR CAD CHF MXN HKD JPY INR NOK SEK RUB
exports.CURRENCIES = [
    'KRW', 'EUR', 'GBP', 'AUD',
    'USD', 'TRY', 'ZAR', 'CAD',
    'CHF', 'MXN', 'HKD', 'JPY',
    'INR', 'NOK', 'SEK', 'RUB'
];
/**
 * Forex contract
 * @param symbol
 * @param currency
 */
function forex(symbol, currency) {
    var temp;
    // Swap between symbol and currency if the ordering is incorrect.
    if (exports.CURRENCIES.indexOf(symbol) > exports.CURRENCIES.indexOf(currency)) {
        temp = symbol;
        symbol = currency;
        currency = temp;
    }
    return {
        currency: currency,
        exchange: 'IDEALPRO',
        secType: 'CASH',
        symbol: symbol
    };
}
exports.forex = forex;
;
//# sourceMappingURL=forex.js.map